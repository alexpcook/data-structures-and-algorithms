package linkedlist

// A singly linked list contains a set of items, each with a pointer to the next item.
// - Prepend O(1)
// - Append O(1)
// - Lookup O(n)
// - Insert O(n)
// - Delete O(n)

// A doubly linked list contains a set of items, each with a pointer to the next item
// and a pointer to the previous item. This can make lookup, insert, and delete slightly
// more efficient (O(n/2) instead of O(n)) if we know in which half of the list the value resides.
// - Prepend O(1)
// - Append O(1)
// - Lookup O(n)
// - Insert O(n)
// - Delete O(n)
